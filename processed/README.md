This directory contains intermediate artifacts generated during preprocessing and retrieval.

- chunks.jsonl: segmented text chunks
- embeddings.npy: vector embeddings
- embeddings_meta.jsonl: metadata for each chunk
- faiss.index: FAISS index used for retrieval

These files are generated automatically and can be reproduced.